import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // Es ist keine weitere Konfiguration für diese App erforderlich.
  // Vite behandelt die meisten Dinge, einschließlich Umgebungsvariablen wie import.meta.env.VITE_API_KEY, automatisch.
});
